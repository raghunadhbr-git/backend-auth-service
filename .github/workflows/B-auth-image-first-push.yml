name: 🚀 Build & Push First ---> backend-auth-service image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      # ==============================
      # 📥 Checkout Repository
      # ==============================
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      # ==============================
      # 🔐 Docker Login
      # ==============================
      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ==============================
      # 🧠 Show Info
      # ==============================
      - name: 🧠 Show Build Info
        run: |
          echo "🚀 Starting Docker Build for Auth Service"
          echo "📦 Image: pythoncodelife/backend-auth-service:latest"
          echo "🕒 Time: $(date)"

      # ==============================
      # 🐳 Build Docker Image
      # ==============================
      - name: 🐳 Build Docker Image
        run: |
          docker build -t pythoncodelife/backend-auth-service:latest .

      # ==============================
      # 📤 Push Docker Image
      # ==============================
      - name: 📤 Push Docker Image
        run: |
          docker push pythoncodelife/backend-auth-service:latest

      # ==============================
      # 🔍 Verify Image
      # ==============================
      - name: 🔍 Verify Image Push
        run: |
          echo "✅ Image pushed successfully!"
          echo "🔗 DockerHub: https://hub.docker.com/r/pythoncodelife/backend-auth-service"
      
      # ==============================
      # 🎉 Done
      # ==============================
      - name: 🎉 Done
        run: |
          echo "🚀 backend-auth-service Docker Image Ready for Deployment"
