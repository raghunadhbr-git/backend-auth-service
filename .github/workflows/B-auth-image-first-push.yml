name: 🚀 Build & Push First ---> Auth Service Image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      # ==============================
      # 📥 Checkout Repository
      # ==============================
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      # ==============================
      # 🔐 Docker Login
      # ==============================
      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ==============================
      # 🧠 Show Info
      # ==============================
      - name: 🧠 Show Build Info
        run: |
          echo "🚀 Starting Docker Build for Auth Service"
          echo "📦 Image: pythoncodelife/auth-service:latest"
          echo "🕒 Time: $(date)"

      # ==============================
      # 🐳 Build Docker Image
      # ==============================
      - name: 🐳 Build Docker Image
        run: |
          docker build -t pythoncodelife/auth-service:latest .

      # ==============================
      # 📤 Push Docker Image
      # ==============================
      - name: 📤 Push Docker Image
        run: |
          docker push pythoncodelife/auth-service:latest

      # ==============================
      # 🔍 Verify Image
      # ==============================
      - name: 🔍 Verify Image Push
        run: |
          echo "✅ Image pushed successfully!"
          echo "🔗 DockerHub: https://hub.docker.com/r/pythoncodelife/auth-service"
      
      # ==============================
      # 🎉 Done
      # ==============================
      - name: 🎉 Done
        run: |
          echo "🚀 Auth Service Docker Image Ready for Deployment"
